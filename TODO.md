# TODO

- [ ] Before mainnet deploy: change the `gutenberg_registry` upgrade authority
      off my personal `~/.config/solana/id.json` key and decide the final
      posture (multisig, hardware wallet, or `set-upgrade-authority --final`).
      Otherwise the "permanent" promise in the README is only as strong as
      that one keypair.
