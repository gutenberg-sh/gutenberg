export {
  resolve_content_url,
  build_gateway_entries,
  resolve_content_links,
  fetch_blob,
} from '@gutenberg/core';
export type {
  GatewayKind,
  GatewayEntry,
  ResolvedGatewayLink,
  GatewayValidation,
  GatewayValidator,
} from '@gutenberg/core';
